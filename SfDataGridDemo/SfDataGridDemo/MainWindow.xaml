<Window
    x:Class="SfDataGridDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SfDataGridDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:syncfusion="using:Syncfusion.UI.Xaml.DataGrid"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <syncfusion:SfDataGrid x:Name="dataGrid"   
                               AllowFiltering="True"
                               AllowEditing="True"
                               ColumnWidthMode="Star"                               
                               ItemsSource="{Binding ProductInfo}">
            <syncfusion:SfDataGrid.DataContext>
                <local:ProductSalesDetailsCollection />
            </syncfusion:SfDataGrid.DataContext>            
        </syncfusion:SfDataGrid>
        <StackPanel Grid.Column="1">
            <Button x:Name="Serialization" Width="150" Height="40" Margin="5" Content="Serialization" Click="OnSerializationClicked"/>
            <Button x:Name="Deserialization" Width="150" Height="40" Margin="5" Content="DeSerialization" Click="OnDeSerializationClicked"/>
        </StackPanel>
    </Grid>
</Window>
